# Storefront UX QA — 2026-09-19

final result: passed

## 范围与视觉基准

本次是现有商城交互优化，保留原有字体、布局、插画、配色和商品内容。目标是统一四处下拉框，并完善现有页面的状态反馈，不是复制系统默认菜单。

- 原始问题截图：`docs/ux/source-select.png`（364 × 480 px，用户裁图，原 CSS 尺寸及密度未知，仅比较菜单外观，不作像素尺寸推断）。
- 全页基准：`docs/ux/before-catalog.png`；结果：`docs/ux/after-catalog.png`。两张为 1440 × 1000 px / CSS viewport 1440 × 1000，截图有效密度 1，没有缩放处理。
- 路由 `/products`，全部商品、所有品牌、编辑推荐。全页截图上方均已滚动，滚动位置有数像素差异；原生弹出菜单未被浏览器截图捕获，所以原生展开态用用户裁图补充。收藏数据随同一浏览器中的操作变化，不计为视觉差异。
- 手机：390 × 844 px / CSS viewport 390 × 844，截图有效密度 1。`after-catalog-mobile.png` 为品牌展开；`after-review-mobile.png` 为订单评价评分展开；`after-error-mobile.png` 为地址校验失败。
- 比较方式：原始与实现图已放在同一次图像输入中比较；另将用户菜单裁图、桌面菜单及手机菜单一起比较。聚焦区域为品牌菜单、评价弹窗和表单错误，可直接读出字体、边框、状态色和控件间距。

## 修复与复核记录

1. [P2 / 已修复] 品牌、排序、订单状态、评分原生菜单外观不统一。替换为同一个 StoreSelect，浅绿选中态、细边框、短展开动画、轻阴影。桌面和手机截图复核：没有页面横向溢出；390 px 下品牌菜单位于 x=48..208，处于视口内。评分菜单未越出弹窗。
2. [P2 / 已修复] 校验失败提示被原生 dialog 遮罩挡住。MotionDialog 在弹窗内部保留错误并将焦点移至提示；Toast 到时消失后错误仍保留。错误状态截图见 `after-error-mobile.png`。
3. [P2 / 已修复] 切页过渡结束可能抢走用户刚落在菜单上的焦点。现在只有焦点未进入主内容时才定位页面标题，筛选查询也不重新挂载页面。
4. [P2 / 已修复] 优惠码输入框按 Enter 会提交整个订单。现在 Enter 只验证、应用优惠码；INVALID 与 HELLO10 两种状态均已通过浏览器操作验证，页面仍停留在 checkout。
5. [用户批注 / 已完成] 商品名称下划线以文字宽度为准，中心向左右展开，移开向中心收回，200ms linear。共享商品卡适用于首页、列表、收藏及推荐区。浏览器 computed style 确认中心为文字宽度的一半（108.640625 / 2），hover 前后 scaleX 为 0 → 1 → 0，缓动为 linear。

## 五项视觉检查

- 字体：保留原站标题与正文；下拉使用现有正文，12px、合理行高；手机选项触控高度 44px。没有文字被截断。
- 间距与布局：保留商品网格与页面留白；桌面排序控件统一为 44px 高，因此第一行商品略下移，此为有意调整。菜单浮层不撑开页面。
- 颜色：沿用深绿文字、浅绿状态、暖色操作按钮；错误用红色语义提示，选中态不再使用系统蓝。
- 图片：沿用已有 ProductArt 与 Icon，没有新造或替换图片资源；SVG 仍清晰。
- 文案：原站业务文案保持，新增的是明确的操作状态（已加入、撤销、校验提示），没有将设计提示词暴露到产品界面。

## 浏览器交互与自动验证

- 品牌 Home / End、方向键、Enter、Esc、Tab、输入字母定位；筛选结果正确，Tab 可离开控件。
- 订单状态菜单筛选已送达；打开订单与评分菜单。
- 评价菜单第一下 Esc 只收起菜单，第二下 Esc 关闭弹窗并将焦点还给“评价商品”。
- 商品加购按钮反馈；购物袋移除最后一件、焦点定位撤销、恢复原商品和金额。
- 优惠码失败原位提示、成功后金额更新；地址空白姓名被阻止保存。
- prefers-reduced-motion 模拟：下划线 transition 变为 0s，验证后已恢复媒体和视口设置。
- 已检查控制台，无 error / warn。`pnpm build` 成功；`pnpm test` 8 项通过。

## 实现清单与边界

- [x] 首页和各路由共享页面转场、按压反馈、收藏反馈。
- [x] 商品筛选重排、规格价格反馈、加购成功状态。
- [x] 购物袋删除 / 撤销，结算选中态、优惠反馈。
- [x] 账户、地址、评价和确认弹窗进入 / 退出；错误提示保留。
- [x] 原生 dialog 焦点约束、键盘下拉、减少动态效果。

无未解决 P0 / P1 / P2。未做真实移动设备与独立屏幕阅读器验收；手机验证为浏览器响应式视口。数据继续使用既有本地 mock，没有连接真实支付或数据库，也没有增加运行依赖。
