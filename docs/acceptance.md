> 历史设计记录：此文件描述早期 Mock 阶段；当前实际实现和接入方式以根 README 与 [API 接入提示词](api-integration-prompt.md) 为准。

# 验收记录

2026-09-19，本地 `127.0.0.1:5174`。

## 自动检查

- `pnpm build`：生产构建通过。
- `pnpm test`：6 组 Node 原生测试通过，覆盖混合税率/折扣分摊/总额、数量和库存边界、幂等创建、地址快照、取消释放、支付失败重试/重复支付、评价购买关系/状态/防重复、持久化失败回滚、库存冲突回滚及地址格式。
- 无额外测试框架，没有宣称运行 TypeScript 或 Lint。

## 真实浏览器交互

- 桌面 1440px：首页和商品页展示正常。
- 选阅读灯深色两件 → 购物袋 → 示例账户 → HELLO10：税前 ¥12,800、优惠 ¥1,280、税 ¥1,152、免运费、应付 ¥12,672。
- 创建订单 → 模拟支付失败 → 重试成功 → 刷新：付款状态、快照和金额保留。
- 已送达历史订单提交文字评价后显示待审核，提交按钮禁用。
- 手机 390×844：地址弹窗可见、可编辑保存；结算使用新的地址。
- 搜索青山焙茶，售罄按钮禁用；收藏后在收藏页出现。
- 商品第二页切换、价格升序、有货筛选正常；售罄商品被过滤。
- 咖啡小额订单的 HELLO10 门槛被拒绝；无优惠下单并确认取消，详情为已取消，SKU 可售库存恢复到 22。
- 首页、商品列表、订单详情的手机 document.scrollWidth 均为 390，没有整页横向溢出；导航和时间线允许组件自身横向滚动。
- 已修复 Vue Router 忽略 query 导致多个分类导航同时高亮的问题，改为根据 category 明确选中。
- 检查开发控制台，没有发现运行时 warning/error。

浏览器验收由工具实际点击完成，未建立独立 Playwright 自动化套件。Mock 流程通过不代表真实 API、数据库事务、支付网关或生产登录已完成。
